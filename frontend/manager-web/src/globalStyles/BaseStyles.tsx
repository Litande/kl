import styled from "styled-components";
import { borderStyle } from "globalStyles/theme/border";

export const WrapHeader = styled.h2`
  margin: 0 1.5rem 1.5rem 0;
  padding-bottom: 1rem;
  border-bottom: ${borderStyle.primary};
`;
