export enum ROUTES {
  DASHBOARD = "/",
  DIALING = "dial",
  FUTURE_CALLBACK = "callbacks",
  MANUAL_CALL = "manual",
  HISTORY = "history",
  AUTH = "auth",
  NOT_FOUND = "*",
}

export enum ROLES {
  AGENT = "AGENT",
}
